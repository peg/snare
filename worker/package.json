{
  "name": "snare-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "cf:check": "wrangler deploy --dry-run",
    "cf:deploy": "wrangler deploy --keep-vars --strict"
  },
  "devDependencies": {
    "vitest": "4.1.10",
    "wrangler": "4.114.0"
  }
}
